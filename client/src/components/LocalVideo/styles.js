import { css } from 'linaria';

const video = css`
  position: fixed;
  bottom: 1.5rem;
  right: 1.5rem;
  width: 20%;
  height: auto;
  border-radius: var(--radius-light);
  z-index: 10;
`;

export default { video };
