import { css } from "linaria";

const wrapper = css`
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100%;

  video {
    max-width: 100%;
  }
`;

export default { wrapper };
